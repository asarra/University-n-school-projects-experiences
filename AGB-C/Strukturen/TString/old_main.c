    /*
    typedef struct{
    char *a;
    }TString;

    TString *a="August-Griese Berufskolleg";
    TString *b="Khudadad, ich laufe!";

    printf("%s\t%s",a,b);
    ///"gcc auto allocates a string when you use the non-standard %a"
    TString *c;         ///char *a bringt nur etwas, wenn man ohne Eingabe initialisiert! Experimentresultat.
    ///scanf("%s",c);  /// Als Überprüfung! Die Eingabe in scanf wird nicht vernünftig ausgegeben. Heißt aber auch, dass es nicht vernünftig gespeichert wird!
    ///printf("\n%s",c);
    */
    ///Als nächstes Experiment "char array[]" ich es.
